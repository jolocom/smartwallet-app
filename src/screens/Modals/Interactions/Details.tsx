import React from 'react';

import ScreenContainer from '~/components/ScreenContainer';
import Header from '~/components/Header';

const Details: React.FC = () => {
  return (
    <ScreenContainer>
      <Header>Interaction Details</Header>
    </ScreenContainer>
  );
};

export default Details;
