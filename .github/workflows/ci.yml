name: Run tests for SW 2.0:

on: [push]

jobs:
  install-and-test:
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v2

      - name: Install npm dependencies
        run: |
          yarn install

      - name: Run tests
        run: |
          yarn test
